name: Run Pester Tests and Publish to PowerShell Gallery

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install Pester
      shell: pwsh
      run: Install-Module Pester -Force
    - name: Run Pester tests
      shell: pwsh
      run: Invoke-Pester -OutputFormat NUnitXml -OutputFile test-results.xml
    - name: Publish to PowerShell Gallery
      if: success()
      shell: pwsh
      run: Publish-Module -Name 'PSGraphTest' -NuGetApiKey '${{ secrets.PSGALLERY_TOKEN }}'
